/**
 * This is an interesting collection task from the interview
 * Finance technology centre
 * We have several arrays (two at least) working FIFO  and several crossing nodes
 * As we put element inside collection all the elements moves along the queue
 * So we have to mark element which is belong to several collections which replace
 * element from the other collection
 */

package com.site.dna;